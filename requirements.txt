# Website (Flask) and Auth0 login
flask>=3.0
authlib
requests

# Video calls + chat (FastAPI)
fastapi
uvicorn
python-multipart

# AI transcripts and notes
google-genai
pydantic

# Database - user data and matching (TigerData / Postgres)
psycopg[binary,pool]>=3.2

# Shared
python-dotenv
